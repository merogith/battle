# Python deps for the RL bridge (Stage 1) and first training run (Stage 2).
# Install with:  pip install -r ml/requirements.txt
numpy>=1.24
gymnasium>=0.29

# --- Stage 2 (uncomment when you start training) ---------------------------
# stable-baselines3>=2.3      # batteries-included PPO/DQN
# sb3-contrib>=2.3            # MaskablePPO (uses the legal-action mask)
# tensorboard>=2.15           # watch win-rate climb during training
