Per-agent finding reports land here as `<agent-name>-<timestamp>.md`. Read by
`scripts/debug/issue-ledger.mjs` and collapsed into `../ISSUE_LEDGER.md`. Safe
to delete a stale file — the ledger regenerates from whatever is present.
